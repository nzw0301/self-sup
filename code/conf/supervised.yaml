defaults:
    - _self_
    - backbone: resnet18
    - augmentation: simclr
    - dataset: cifar100
    - distributed: base
    - experiment: training/cifar
    - lr_scheduler: simclr
    - optimizer: supervised
    - override hydra/output: supervised

name: supervised
hydra/job/chdir: true
